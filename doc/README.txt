rope (Relational Object Publishing Environment)
-----------------------------------------------

Publish data stored in relational database.

The data is accessed through SQLAlchemy.

Zope specific metadata like permissions or local roles can be stored
in a separate table thanks to SQLAlchemy. 

Look in tests for examples how to use the base classes.


